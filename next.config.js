/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    unoptimized: true,
  },
  output: "export",
  // basePath: "/new",
  //   assetPrefix: "/new/"
};
module.exports = nextConfig;
